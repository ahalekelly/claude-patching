# Patches applied without a behavioral test. run-all.sh fails on any applied
# patch it has no test for unless the patch is named here, so every entry has to
# say why a test cannot exist yet — a waiver is a debt, not a decision.
#
#   <patch-id> <reason>

agent-list-models  the agent list only draws rows for live subagent sessions, so this needs a live-model test in the shape of tests/mcp-per-subagent/run.py
